{
  "name": "couture-mali-rules-tests",
  "private": true,
  "description": "Security-rules tests proving the secretary can never access financial data.",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project demo-couture-mali \"mocha rules_tests/test --timeout 30000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "firebase-tools": "^14.1.0",
    "mocha": "^10.7.3"
  }
}
